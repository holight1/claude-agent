# review-task — 任务返回处理流程

DS 完成任务后，架构师执行以下步骤处理返回结果。

---

## 1. 拉取任务文件（远端任务）

若任务 `**执行环境**` 含 `远端`，先从远端拉取最新版本（完成区由 DS 填写）：

```bash
# 从路由表反推 pull 命令（push 的 scp 命令反向即可）
scp [-P port] user@host:~/repo/code-agent/tasks/<file> ~/repo/code-agent/tasks/<file>
```

拉取失败时报错，不继续。

## 2. 读取完成区

读取任务文件的 `## 完成区`，确认：
- `**状态**`：已完成 / 部分完成 / 失败
- `**修改文件**`：列出的变更
- `**遗留问题**`：是否有 blocker

若完成区仍为模板占位（DS 尚未填写），提示用户任务结果还未写回。

## 3. Review 代码（有代码改动时）

```bash
git status          # 了解改动范围；重点看有没有 D（删了之前能工作的文件/hook = 回归高频根因）
```

按完成区列出的文件逐一读取，检查逻辑是否与任务设计一致。

**独立复跑，不只读（关键）**：验收以**架构师亲自重跑的 ground-truth 为准**，完成区和自填的 `## Codex Review` 只是线索、可能与实际树不符。重 build、重跑测试/差分，`cmd; echo $?` 核退出码。「编过」≠「能跑」。防「凑绿」：`|| true` 吞退出码、删文件解锁、伪造构建/运行输出——一律打回。易造假域（模拟器内部、编译器 CodeGen）尤其亲自 build + 跑，任务文件里应已放硬门槛（贴真实输出 + 负测试）。

**Review 分类**：

| 任务类型 | Review 重点 |
|---------|-------------|
| 通过已有测试 | 检查 pass/fail 结果即可 |
| 新增测试 | 重点 review CHECK 语义是否正确（核心职责）|

**处理方式**：

| 问题类型 | 处理 |
|---------|------|
| 测试断言口径、漏改引用、格式/命名等轻量问题 | 直接修复 + 运行验收 |
| 设计不符、ABI 破坏、跨模块重构等阻断问题 | 报告用户，视情况下发修复任务 |

## 4. 更新 memory 和知识库

- 若有新的技术结论（API 用法、踩坑、架构决策）→ 更新对应知识库章节
- 若改变了项目状态/进展 → 更新架构师的 memory 索引（MEMORY.md 摘要行；细节多时可拆到独立子文件，如 project_*.md）
- `10-changelog.md` 追加一行记录（格式：`| 日期 | 描述 | DS/架构师 |`）
- 纯测试修复、注释调整、知识库已覆盖 → 无需更新

## 5. Commit

Review 无阻断问题、轻量问题已修复、验收命令通过后：

```bash
git status          # 只纳入本任务相关文件
git add <files>
git commit -m "<task-id>: 描述"
```

不自动 push，push 须用户确认。
