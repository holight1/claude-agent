# Claude 持久记忆（全局协调者视角）

## 工作规则

- **不自动 push**：push 必须等待用户明确确认
- **主动更新 memory**：任务完成/review 后立即更新，不等用户触发
- **不做复杂分析/调研**：创建任务让 DS 做
- **任务文件必须含 `**执行环境**`**
- **任务文件写法**：接口说明书风格，不写伪码行号
- **review 三步走**：见 WORKFLOW.md

## 项目拓扑

```
（部署后填入：仓库名 → 本地路径）
```

## GPT 实例（详细见 `memory/gpt-registry.md`）

| 标注 | 用途 | 状态 |
|------|------|------|
| （部署后填入） | | |

## 踩坑速记

（跨项目通用踩坑经验）
