---
name: task-acceptance-replay
description: Use when an executor returns a task and you are about to mark it passed, or before committing an executor's output — establishes ground truth by re-running verification yourself, selects which checks the change actually requires, and rejects green results that did not act on the delivered object.
---

# 任务验收复跑

**状态**：enabled
**消费者**：共享
**投影**：DS-common.md
**来源**：dsh `dsh-pre-push-checks` + 本框架八起真实验收事故 + sim process-note 0010 / 0011
**核心判断**：这条绿色结果，证明的是不是我声称的那件事？

**本 skill 是判据，不是完整清单。** 判据的**由来与实测事故**不在正文里，在下方「判据来源」的记录与 `evals/EVALS.md` 里——正文只留可执行的检查。

## 判据来源

- `decisions/2026-08-21-contract-blindspot.md` — §5 反例证伪、§6 留一部分不依赖任务书
- `decisions/2026-08-21-vacuous-bound-assertion.md` — 核四样的第四问、对侧约束与同源、口径偷换
- `decisions/2026-08-21-self-report-needs-evidence-command.md` — 整改记录逐条复核、「查不到」才算删除证据
- `decisions/2026-08-21-adopt-sim-process-notes.md` — 终审最后一步指回任务设计
- `decisions/2026-08-21-executor-commit-boundary.md` — §0 定位里的三种写入策略
- `evals/EVALS.md` — 每条判据对应的具体场景与实测数字

## 跨角色必读

### 验收对象不可偷换

**验收命令必须真实 exercise 任务要测的那个对象**，不得为"过门槛"而偷换或缩小被测对象：

| 严禁 | 例 |
|---|---|
| 用手搓/旁路产物替代真实被测系统 | 手写一个能过命令的结果，绕过本该被测的工具/流程 |
| 缩小到不触发问题的更简单情形 | 任务要测 A+B 交互，只测 A 让命令通过 |
| 让命令"绿"但没测到目标 | 退出码/输出对了，但来源不是任务要验证的路径 |
| **同一份代码，但调用方与实现的单位/时基口径不一致** | 差几个数量级而两侧各自自洽。**这是无意的偷换**，写测试的人自己不知道 |

**量纲以被调用者的签名/规格为准，不得以调用点的注释为准。** 照注释核量纲会得到一个自洽的错误答案——它比不核更危险，因为它留下了「已核对」的痕迹。

**负测试的数字必须能用交付实现的签名复算出来。复算差整数个数量级 ⇒ 它跑的不是交付物。**

### 一条绿色结果成立的四个条件

1. **退出码取自被验证的那条命令**——避免管道，或用 `${PIPESTATUS[0]}`；`cmd | tail; echo $?` 取到的是 `tail` 的
2. **处理单元数不为 0**——校验工具要打印它处理了多少单元；**0 个 = 该验证无效**，不是通过
3. **作用于真实交付入口**——流程里若有 restore / reset / fixture 重建，被测对象可能在跑之前已被治好
4. **被约束量的量级落在断言的作用域里**——问：*这个数离断言的界有多远？* 差整数个数量级 ⇒ **该断言无信息量**，通过不构成证据

对每条绿色结果问一次：**如果实现是错的，这条命令会不会红？** 答不上来 ⇒ 它不构成证据。

### 声称「撤下 / 删除 / 改名」时，证据必须是「查不到」

不是「我改了」。要贴 `grep -c` 为 0，或 diff 里的删除行。

**在只需要动一行文字的动作上，决定与执行之间的距离短到人会以为自己已经做了**——所以这类整改最容易只写不做。

---
## 何时用

**触发**：执行端返回任务；准备把任务标成通过；准备 commit 执行端的产出；准备把某条结论写进持久文档。

**不触发**：自己刚写的单行改动；本轮已亲自跑过且工作树未变的重复确认。

## 范围合并

本 skill 吸收了 `relevant-check-selection`（dsh 的 `dsh-pre-push-checks`）。dsh 的落地终点是远端 PR + CI，选检查项是执行端 push 前的事；本框架的执行端不 push，**架构师才是门禁**，选检查和跑检查是同一个人同一时刻的两半。

## 要防的诱人错误

这些不是疏忽。每一条都有正当的推理路径，下一个人会独立地重新犯，而反驳它需要现场没有的信息。第三列是**该问的那一句**；每条对应的实测事故见 `evals/EVALS.md`。

| 诱人错误 | 它为什么看起来对 | 现场缺什么（该问什么） |
|---|---|---|
| **「X 通过」=「X 正确」** | 命令退出 0 了 | 这条命令在实现是错的时候会不会红 |
| 校验作用于**错误的对象** | 脚本跑完了，报了个漂亮的数 | 它到底处理了几个单元——提取器的格式假设与被校验文件的实际格式对得上吗 |
| 门禁**空转**仍报绿 | 逐条都是 OK | 流程里有没有 restore / reset / fixture 重建，把被测对象在跑之前治好了 |
| 数字没有**产生它的命令** | 数字来自完成区或口述，看着具体 | 这个数统计的**单元**是什么，有没有把别名、译文、重复项算进去 |
| **跨仓错位** | 规则读起来很合理 | 它是**哪个仓**的事实；换个仓还成立吗 |
| 退出码取自**管道末端** | `cmd \| tail; echo $?` 写法很自然 | `$?` 是 `tail` 的，不是 `cmd` 的 |
| 完成区自审当证据 | 执行端逐条打了勾 | 那是线索，不是 ground truth |
| **上下界断言通过 = 该性质被守护** | 断言写着界，测试名字也点了那个机制 | 被约束量离界几个数量级；把界放宽到无穷，断言还通过吗 |
| **负测试贴了实跑输出 = 负侧成立** | 数字很具体，不是「我认为会红」 | 那个数能否用**交付实现的签名**复算出来 |
| **整改记录里的动作 = 已发生** | 「已撤下 / 已改名」写得斩钉截铁 | 整改常不产生代码 diff（改的是任务文件本身），`git show --stat` 看不出；要 `grep -n` 查它是否真的**查不到** |

## 判断程序

### 0. 定位：我在哪个仓，这个仓应该长什么样

本框架的架构师**坐在仓库之上**，一个会话里同时碰多个仓——这一面 dsh 的 skill 没有。

- 所有 git 命令**显式带仓路径**（`git -C <repo> …`），不依赖 cwd
- 先取该仓的写入策略（见项目 `DS.md`），它决定「健康长什么样」：

| 仓类型 | 复跑前应看到 |
|---|---|
| 自有仓，执行端可 commit | 恰好一个属于本任务的新 commit，箱外无残留 |
| 上游 clone，禁止 commit | `git status --short` **为空**；非空即事故 |
| 产出挂在仓外（符号链接） | `git status` **看不见它** —— 它的证据必须另外取，别把「干净」当成「没产出」 |

### 1. 先看减法，再看加法

`D` 状态是回归的高频根因：执行端为解锁当前任务删掉了之前能工作的东西。

- 找删除项
- `git show --stat <sha>` 对照完成区的修改文件清单
- **箱外残留 = 完成区不完整** → 打回，不要替它补 commit

**⚠️ 整改记录声称的删除动作要单独复核，`git show --stat` 看不出来。** review 后的整改往往改的是**任务文件自己**（撤下一行清单、改一句措辞），不产生代码 diff。对整改记录里每一条「撤下 / 删除 / 改名」，自己跑一次 `grep -n` 看它是否真的查不到。

⚠️ **逐条查，不能抽样。** 已发生的形态是同一份整改记录里一条真做了、另一条只写没做——**不是整体敷衍，是单条落空**，所以抽到一条真的不构成对整段的信任。

### 2. 选检查项

从 outgoing diff 推：改了哪一层 → 该层**最窄而充分**的检查。

- 不盲跑全套（掩盖归因，也掩盖「这一项其实没覆盖到」）
- 不重复本轮已经取得的证据
- **每一项必须能一句话回答「它证明了什么」**；写不出来就别跑，跑了也不构成证据

### 3. 复跑，核四样

命令、**退出码**、**处理单元数**、**被约束量的量级**。

- 退出码要取自被验证的那条命令：避免管道，或用 `${PIPESTATUS[0]}`
- **处理单元数为 0 = 该验证无效**，不是通过。要求校验工具打印它处理了多少单元
- 输出里的每个数字，要能对上它声称处理的对象
- **第四样只对「x ≤ B」「x ≥ B」这类断言问**：*这个数离界有多远？*
  - 差整数个数量级 ⇒ **该断言无信息量**，它的「通过」不是证据
  - 交叉验证一次：**把界放宽到无穷，断言还通过吗？** 还通过 ⇒ 那个机制在这条测试里从未被触发
  - 单向断言天然只在越界时说话：**「永远远离界」和「被机制拦在界处」在它看来一模一样**
  - ⚠️ 只对**声称某机制把量拦在界内**的断言这么要求（钳位 / 饱和 / 限速 / 配额）。纯不变量（下标不越界）大余量正是期望结果，逼它逼近界是错的
  - ⚠️ 修法是**对侧**约束：`x ≤ B` 配 `x ≥ k·B`；`x ≥ B` 配 `x ≤ B/k`。给 `x ≥ B` 补 `x ≥ k·B` 是恒真式，别接受这种整改
  - ⚠️ 对侧约束的**构造参数必须由界推出**，不得写死。不同源 ⇒ 界一变测试红在错的地方，报错还指错方向

**同时核一次口径**：负测试贴的数字，用交付实现的签名手工复算一遍。复算差整数个数量级 ⇒ 它跑的不是交付物（见「验收对象不可偷换」末两段）。

### 4. 判定这条证据是不是真的

对每条绿色结果问一次：

> **如果实现是错的，这条命令会不会红？**

答不上来 → 它不构成证据。常见的答不上来的形态：

- 流程里有 restore / reset / fixture 重建，被测对象在跑之前被治好了
- 跑的是手搓产物、Mock、缩小到不触发问题的样例
- 走的是旁路入口，不是真实交付入口
- 只跑不核：命令跑了，但没人看输出内容

### 5. 反例证伪

每条被声称关闭的验收项，**至少破坏一次让它变红**。做不到的那条，说明它当初就不是验收条件。

判据写成一句可执行的话：

> **把实现换成一个明显错误的版本，这条测试会不会失败？**
> **不会 ⇒ 它没有在守护它声称的性质。**

⚠️ **这一问预设「实现有错」，所以它抓不到实现正确、测试构造不到位的那一类。** 上下界断言就是这一类：实现忠于规格，而测试从未把被测量推进断言的作用域，于是把界放宽到无穷（坏实现）与原界给出同一个数、断言双双通过。⇒ 反例证伪之外还要问核四样的第四问（离界多远），两问都要问。

不必真改源码，写几行临时脚本用坏实现跑一遍断言逻辑即可。

**优先证伪对象的清单不在这里**——它由 `semantic-code-review §2.1` 单独维护，本 skill 不再抄一份。清单比原则管用，但**两份清单比没有清单更糟**。

**反向也查**：对每条验收点问「实现若写错，现有哪条用例会红」——答不上来就是缺口。

### 6. 留一部分不依赖任务书

**以任务书为唯一判据的验收，原理上抓不到任务书本身的缺陷。**

判据来源是任务书，而任务书是架构师自己写的——这条链上没有任何一环以任务书为审查对象。它不是谁失职，是流水线的固有盲区。

⇒ 每次终审**至少留一部分不对着验收表打勾**：挑 2–3 个关键数值或算法，**自己独立算一遍**，与实现输出对照；关键常量回查出处；回到设计基线与规格文件核对。

已反复出现的形态：某轮终审的**全部**发现都来自这一步，而同一批产出此前被执行端自审与独立 reviewer 都判过「通过」——因为**三方对着同一份残缺规格，各自都是正确的**。

> §5 与 §6 的来由与原始证据：`~/claude-agent/decisions/2026-08-21-contract-blindspot.md`

## 停止条件

- 任一步拿不到证据 → **停**，在报告里写进「我无法自我验证」，不要用推断补
- 出现删除项或箱外残留 → 直接打回
- 需要修改被验收的对象才能跑通 → 停；这已经不是验收

## 报告格式

三类分开写，不混在一起：

- **我核对到不对**（有命令与输出）
- **我认为不合适**（判断，无实据）
- **我无法自我验证**（必填；空着会让「通过」被读成全面核对过）

逐条验收表：

| 验收项 | 命令 | 退出码 | 处理单元数 | 被约束量/界 | 结论 |
|---|---|---|---|---|---|

「被约束量/界」只对上下界断言填；差整数个数量级填「空转」，不要填「通过」。

### 🔴 打回指令也是合同，写完要过一遍充分性检查

打回不是「指出问题」就结束了——**它规定了执行端接下来要做什么，与任务书同性质**。所以每条整改要求写完后，按 `task-contract-design §1` 的充分性判据自检：

> **把这条整改要求交给一个只想最省力通过的执行端，它能给出的最弱实现是什么？那个最弱实现可接受吗？**

已发生的形态：打回写「补下界：量必须逼近界（例如 ≥ 半个界）」，执行端**一次改对且精确执行字面**——而字面允许的最弱实现是把那个阈值**写死**，于是阈值与被约束量不同源，界一变就红在错的地方。

⇒ **那一轮唯一的新问题来自架构师的打回指令写得不完整，不是执行端偷懒。** 本 skill 已有的判断是「执行端不多不少满足字面 ⇒ 漏洞是任务设计的」；打回指令同理，且**更容易漏**——它写在终审现场、篇幅短、没有人再 review 它。

## 不属于本 skill

- 代码语义缺陷 → `semantic-code-review`
- 引文、计数与事实核对 → `research-evidence-audit`
- 任务当初该怎么写 → `task-contract-design`

## 终审必做的最后一步

**本轮的每个发现，能指回任务设计的哪一条？**

- 指得出 → 记下来，那条判据下次要写死
- **指不出来 → 判据表本身缺项**，按 `process-gap-capture` 落一条 process-note，别只在口头指出

这一步不是可选的收尾。流程缺口写在任务终审区就会散落在几百行任务文件里，采纳时要翻任务，而且**机制依赖有人记得提醒**。

> 本节的来由与原始证据：`~/claude-agent/decisions/2026-08-21-adopt-sim-process-notes.md`
