# Self-hosted framework trace

This directory contains the framework repository's own task contracts,
implementation attempts, review records, decisions, and smoke artifacts.

The authoritative schema is `collab-framework/TRACEABILITY.md`.
